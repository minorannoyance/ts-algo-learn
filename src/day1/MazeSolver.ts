export default function solve(maze: string[], wall: string, start: Point, end: Point): Point[] {

    
    function rec_solve( maze: string[], pos: Point, end: Point ): boolean {
        
        if ( pos.x<0 || pos.x>maze[pos.x] )
    }

    return rec_solve(maze, )
}